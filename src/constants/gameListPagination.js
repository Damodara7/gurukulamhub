/** Card grid page size for game list UIs (3×4 on large screens). */
export const GAME_GRID_PAGE_SIZE = 12

/** Home / landing horizontal carousel preview count (matches quiz landing). */
export const GAME_LANDING_PREVIEW_LIMIT = 8
