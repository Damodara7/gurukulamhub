/**
 * Shared page sizes for quiz list/grid UIs (balances payload size vs. one screen of cards).
 * Grid layouts use 3×4 cards on large screens; admin tables match TablePagination default.
 */
export const QUIZ_GRID_PAGE_SIZE = 12
/** Admin management/quizzes/view card grids */
export const ADMIN_MY_QUIZZES_GRID_PAGE_SIZE = 10
export const QUIZ_LANDING_PREVIEW_LIMIT = 8
export const ADMIN_QUIZ_TABLE_PAGE_SIZE = 10
