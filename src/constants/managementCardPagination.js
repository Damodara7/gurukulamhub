/**
 * Page size for management card grids (audience, group).
 * Large screens: 4 columns × 3 rows = 12 cards per page.
 */
export const MANAGEMENT_CARD_GRID_PAGE_SIZE = 12

/** Full-width admin notification rows per page */
export const MANAGEMENT_NOTIFICATION_LIST_PAGE_SIZE = 8

/** My Groups & Channels list rows per page */
export const MY_GROUPS_LIST_PAGE_SIZE = 8

/** Bottom padding on management list pages so fixed FAB + pagination do not cover cards */
export const MANAGEMENT_CONTENT_BOTTOM_PADDING = { xs: 12, sm: 16 }
